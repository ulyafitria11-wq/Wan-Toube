<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ZulTube 💚</title>
    <style>
      body {
        font-family: 'Poppins', sans-serif;
        background-color: #0d0d0d;
        color: #f1f1f1;
        text-align: center;
        margin: 0;
        padding: 0;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #101010;
        padding: 15px;
        box-shadow: 0 2px 10px rgba(0, 255, 0, 0.1);
      }

      header img {
        width: 40px;
        height: 40px;
        margin-right: 10px;
        border-radius: 8px;
      }

      h1 {
        color: #00ff6a;
        font-size: 24px;
        letter-spacing: 1px;
      }

      input {
        width: 70%;
        padding: 10px;
        margin: 20px 5px;
        border-radius: 8px;
        border: 1px solid #00ff6a;
        background-color: #1a1a1a;
        color: white;
      }

      button {
        padding: 10px 20px;
        border: none;
        background: #00ff6a;
        color: black;
        font-weight: bold;
        border-radius: 8px;
        cursor: pointer;
      }

      .video {
        background: #1a1a1a;
        margin: 15px auto;
        width: 90%;
        border-radius: 12px;
        box-shadow: 0 2px 10px rgba(0, 255, 0, 0.1);
        padding: 10px;
      }

      .video img {
        width: 100%;
        border-radius: 8px;
      }

      iframe {
        width: 90%;
        height: 230px;
        margin-top: 10px;
        border-radius: 8px;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>ZulTube 💚</h1>
    </header>

    <div>
      <input id="search" placeholder="Cari video..." />
      <button onclick="searchVideo()">Cari</button>
    </div>

    <div id="results"></div>

    <script>
      const API_KEY = "AIzaSyC_cjLJnr0-T9yNR9WAe8oRq49V461-goM"; 

      async function searchVideo() {
        const query = document.getElementById("search").value;
        if (!query) return alert("Ketik dulu judul videonya!");

        const url = `https://www.googleapis.com/youtube/v3/search?part=snippet&q=${query}&type=video&maxResults=20&key=${API_KEY}`;
        const response = await fetch(url);
        const data = await response.json();

        let hasil = "";
        data.items.forEach(video => {
          const videoId = video.id.videoId;
          hasil += `
            <div class="video">
              <h3>${video.snippet.title}</h3>
              <img src="${video.snippet.thumbnails.medium.url}" alt="thumbnail" />
              <p>${video.snippet.channelTitle}</p>
              <button onclick="playVideo('${videoId}')">▶️ Play</button>
            </div>
          `;
        });
        document.getElementById("results").innerHTML = hasil;
      }

      function playVideo(id) {
        const frame = `<iframe src="https://www.youtube.com/embed/${id}" frameborder="0" allowfullscreen></iframe>`;
        document.getElementById("results").innerHTML = frame;
      }
    </script>
  </body>
</html>
